<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shaw.mapper.BlogTypeMapper">

    <resultMap type="BlogType" id="BlogTypeResult">
        <result property="id" column="id"/>
        <result property="typeName" column="typeName"/>
        <result property="blogCount" column="blogCount"/>
        <result property="orderNo" column="orderNo"/>
    </resultMap>

    <sql id="Base_Column_List">
		id,typeName,orderNo
	</sql>

    <select id="countList" resultMap="BlogTypeResult">
		SELECT t2.id,t2.typeName,COUNT(t1.id) AS blogCount FROM blog t1 RIGHT JOIN blog_type t2 ON t1.typeId=t2.id GROUP BY t2.typeName order by t2.orderNo;
	</select>

    <select id="findById" parameterType="Integer" resultMap="BlogTypeResult">
        select
        <include refid="Base_Column_List"/>
        from blog_type where id=#{id}
    </select>

    <select id="list" parameterType="Map" resultMap="BlogTypeResult">
        select
        <include refid="Base_Column_List"/>
        from blog_type
        <if test="start!=null and size!=null">
            limit #{start},#{size}
        </if>
    </select>

    <select id="getTotal" parameterType="Map" resultType="Long">
		select count(1) from blog_type
	</select>

    <insert id="add" parameterType="BlogType">
		insert into blog_type values(null,#{typeName},#{orderNo});
	</insert>

    <update id="update" parameterType="BlogType">
        update blog_type
        <set>
            <if test="typeName!=null and typeName!='' ">
                typeName=#{typeName},
            </if>
            <if test="orderNo!=null ">
                orderNo=#{orderNo},
            </if>
        </set>
        where id=#{id}
    </update>

    <delete id="delete" parameterType="Integer">
		delete from blog_type where id=#{id}
	</delete>
</mapper> 
